# Discord bot token — Discord Developer Portal > Bot > Reset Token
DISCORD_TOKEN=

# API key for the configured provider (one of these):
# GLM / OpenAI-compatible endpoint
GLM_API_KEY=
# Anthropic Claude (if llm.provider: anthropic)
ANTHROPIC_API_KEY=
